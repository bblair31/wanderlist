<h1>ALL USERS</h1>

<%= form_tag("/search", method: "get") do %>
  <%= label_tag(:user_name, "Search by User's Name:") %>
  <%= text_field_tag(:user_name) %>
  <%= submit_tag("Search") %>
<% end %>

<% @users.each do |user| %>
  <p><%= link_to user.full_name, user_path(user) %></p>
<% end %>
